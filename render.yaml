services:
  # Frontend - Static Site
  - type: static
    name: growwise
    rootDir: HouseHold-Budgeting/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

